global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "codearena-backend"
    static_configs:
      - targets: ["backend:8000"]
    metrics_path: /metrics
    # If the backend has auth on /metrics, add authorization header here.
